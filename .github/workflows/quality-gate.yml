name: Quality Gate

on:
  pull_request:
  push:
    branches:
      - main
      - "release/**"

jobs:
  lint-type-build:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: sis
          POSTGRES_USER: sis
          POSTGRES_PASSWORD: sis
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -h 127.0.0.1 -U sis -d sis"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    env:
      DATABASE_URL: postgresql://sis:sis@127.0.0.1:5432/sis?schema=public
      E2E_DATABASE_URL: postgresql://sis:sis@127.0.0.1:5432/sis?schema=public
      NEXTAUTH_SECRET: ci-nextauth-secret
      NEXTAUTH_URL: http://localhost:3000
      NEXT_PUBLIC_SITE_URL: http://localhost:3000
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Prepare database
        run: |
          ready=0
          for attempt in $(seq 1 40); do
            if npx prisma db push --skip-generate --accept-data-loss; then
              ready=1
              break
            fi
            sleep 2
          done
          if [ "$ready" -ne 1 ]; then
            echo "Database is not reachable"
            exit 1
          fi
          npm run db:generate
          npm run db:seed
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm run build

  e2e:
    runs-on: ubuntu-latest
    needs: lint-type-build
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: sis
          POSTGRES_USER: sis
          POSTGRES_PASSWORD: sis
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -h 127.0.0.1 -U sis -d sis"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    env:
      DATABASE_URL: postgresql://sis:sis@127.0.0.1:5432/sis?schema=public
      E2E_DATABASE_URL: postgresql://sis:sis@127.0.0.1:5432/sis?schema=public
      NEXTAUTH_SECRET: ci-nextauth-secret
      NEXTAUTH_URL: http://localhost:3000
      NEXT_PUBLIC_SITE_URL: http://localhost:3000
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
